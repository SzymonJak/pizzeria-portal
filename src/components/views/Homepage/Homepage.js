import React from 'react';
import styles from './Homepage.module.scss';

const Homepage = () => (
  <div className={styles.component}>
    <h2>Homepage-dashboard view</h2>
  </div>
);

export default Homepage;